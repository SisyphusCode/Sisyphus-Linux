[service]
name = "udev-trigger"
# Enumerate and add devices after udevd is up. Required so that GPUs, input, disks etc. appear.
exec = "/usr/bin/udevadm"
args = ["trigger", "--action=add"]
after = ["udev"]
type = "oneshot"
restart = "no"

# Graphics and storage services usually require devices to have settled.
