name = "NetworkManager"
description = "Network management daemon"
command = "/usr/libexec/forge/start-networkmanager.sh"
args = ["--no-daemon"]
after = ["dbus", "udev"]
type = "dbus"
bus-name = "org.freedesktop.NetworkManager"
restart = "on-failure"