name = "network-setup"
description = "Network interface setup via NetworkManager"
command = "/usr/libexec/forge/setup-net.sh"
args = []
after = ["dbus", "udev", "udev-settle", "NetworkManager"]
type = "oneshot"
restart = "no"

[environment]
FORGE_NET_IFACE = "wlp82s0"