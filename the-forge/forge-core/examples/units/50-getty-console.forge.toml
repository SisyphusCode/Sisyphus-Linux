[service]
name = "getty-console"
# Serial console (QEMU / hardware serial).
exec = "/usr/libexec/forge/start-agetty.sh"
args = ["--noreset", "-L", "115200", "ttyS0", "vt100"]
after = ["forge-early", "udev", "udev-trigger", "udev-settle"]
restart = "on-failure"