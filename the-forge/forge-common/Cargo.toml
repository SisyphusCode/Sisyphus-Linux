[package]
name = "forge-common"
version = "0.1.0"
edition = "2021"
description = "Shared IPC protocol for The Forge init system"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
