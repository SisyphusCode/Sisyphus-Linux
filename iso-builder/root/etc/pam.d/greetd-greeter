#%PAM-1.0
# Minimal non-interactive greeter session stack for Forge PID 1.

auth            required pam_env.so
auth            required pam_permit.so

account         required pam_permit.so

password        required pam_deny.so

session         required pam_unix.so
session         optional pam_exec.so /usr/libexec/forge/pam-forge-login-session.sh